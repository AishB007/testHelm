apiVersion: v1
kind: Pod
metadata:
  name: mongodb
  labels:
    app: mongodb
spec:
  containers:
    - name: mongodb
      image: quay.io/2210168/mongo:4.0.4
      ports:
        - containerPort: 27017
          name: mongodb-port
